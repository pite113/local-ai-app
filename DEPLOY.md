# 客户部署手册（DEPLOY.md）

> 本文档是给部署人员（你）看的傻瓜化操作手册。目标：在客户电脑上把「本地 AI 工作台」跑起来。

---

## 一、部署前置条件

| 检查项 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11（64位） |
| 内存 | 8GB 以上（推荐 16GB） |
| 磁盘 | 剩余 2GB 以上 |
| 网络 | 装依赖时需要联网一次；之后可离线使用（Ollama 模式） |

---

## 二、先决定"模型模式"（二选一，对客户报价前必须定）

### 模式 A：DeepSeek API（效果最好，按量付费，很便宜）
- 客户需自己注册 DeepSeek：https://platform.deepseek.com（充值 10 元可用很久）
- 优点：效果最好、部署最快（不用下载模型）
- 缺点：客户每月有一点模型费；需要联网

### 模式 B：Ollama 本地模型（免费离线，数据不出门）★ 你的卖点
- 完全免费、离线运行、数据绝不外传
- 需要下载模型包（3B 约 2GB，7B 约 4.5GB）
- 适合：注重数据隐私、不想按月付费的客户
- 部署时多一步：装 Ollama + 拉模型（命令见下）

**建议话术**："数据存在你们自己电脑上，绝不上云；要么每月几块钱用云端最强模型，要么一次性部署、永久免费离线，我帮您装好。"

---

## 三、部署流程（傻瓜版，共 6 步）

### 第 1 步：检查/安装 Python
- 客户电脑打开 `win+r` 输入 `cmd` 回车，输入 `python --version`
- 有显示版本（如 3.10+）→ 跳过
- 报"不是内部或外部命令" → 去 https://www.python.org/downloads 下载安装
  - ⚠️ 安装时**务必勾选** "Add Python to PATH"（加到环境变量）

### 第 2 步：解压交付包
- 把 `local-ai-app_日期.zip` 拷到客户电脑（U盘或网盘）
- 右键解压到一个好找的位置，比如 `C:\local-ai-app\`

### 第 3 步：配置（把 `.env.example` 改名成 `.env` 并填写）
- 进入解压目录，把 `.env.example` 复制一份并改名为 `.env`
- 用记事本打开 `.env`，按第 2 节决定的模式填写：

**模式 A（DeepSeek）：**
```ini
LLM_PROVIDER=openai
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_API_KEY=sk-客户自己的密钥
OPENAI_MODEL=deepseek-chat
```

**模式 B（Ollama 本地）：**
```ini
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b
```

### 第 4 步：启动
- 双击 `start.bat`（第一次会自动装依赖，等 1~3 分钟）
- 看到"本地 AI 工作台已启动" → 浏览器打开 `http://127.0.0.1:8000` 验证
- 验证：问一句"你好"，能回答即成功

### 第 5 步：设置开机自启（客户要求时再做）
- 按 `win+r` 输入 `shell:startup` 回车，打开启动文件夹
- 把 `启动服务.bat` 的快捷方式放进去
- （或部署人员手动运行一次 `启动服务.bat`）

### 第 6 步（可选）：内网全公司访问
- 修改 `.env` 里的 `HOST=0.0.0.0`，重启服务
- 客户电脑查内网 IP：`win+r` → `cmd` → `ipconfig` → IPv4 地址（如 192.168.1.100）
- 公司其他电脑浏览器打开 `http://192.168.1.100:8000` 即可用
- ⚠️ 注意：HOST=0.0.0.0 表示局域网内都能访问，部署前和客户确认安全性

### 模式 B 额外步骤：安装 Ollama
```
① 下载安装: https://ollama.com/download  （安装后任务栏有图标）
② 拉取模型: 打开 cmd 执行: ollama pull qwen2.5:3b
③ 验证: 浏览器打开 http://localhost:11434 有响应即可
```

### 可选：批量作图接真实生图 API

客户要用"②批量作图"出真图时，在 `.env` 加（模型名以硅基流动"模型广场"为准，FLUX.1/SD3.5 已下架）：

```ini
IMAGE_PROVIDER=api
IMAGE_API_BASE_URL=https://api.siliconflow.cn/v1
IMAGE_API_KEY=sk-客户的生图密钥
IMAGE_API_MODEL=Kwai-Kolors/Kolors
```

---

## 四、常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 双击 start.bat 闪退 | Python 没装或没加 PATH | 重装 Python，勾选 "Add to PATH" |
| 浏览器打不开 8000 | 服务没启动 | 重跑 start.bat，看窗口有无报错 |
| 回答报"模型调用失败" | 密钥错/没充值/没联网 | 检查 .env 的 key；DeepSeek 需充值 |
| 问问题不引用文档 | 知识库没上传文档 | 右侧上传 .txt/.docx/.pdf 再问 |
| 上传 PDF 提示无文字 | 是扫描件/图片型 PDF | 用文字版 PDF，或先转成 Word |
| 内网其他电脑访问不了 | HOST 没改 0.0.0.0 / 防火墙 | 改 .env 重启；关防火墙或放行 8000 端口 |

---

## 五、交付清单（走的时候确认）

- [ ] 服务能启动、浏览器能打开
- [ ] 上传一个测试文档、问答正常
- [ ] 三个工具各试一次（批量文本/作图/表格）
- [ ] `.env` 是客户自己的密钥（不是你的！）
- [ ] 教客户：怎么上传文档、怎么提问、怎么重启服务（`启动服务.bat`）
- [ ] 约定维护期（如首月免费，之后按次/按年收费）

---

## 六、安全提醒（重要）

- ❌ 交付包里**绝不能带你的 `.env`**（一键打包已自动排除，自己手动拷时注意）
- ❌ 不要把打包脚本生成的"交付包"文件夹提交到 Git
- ✅ 客户的数据只存在客户电脑的 `data/` 目录里
